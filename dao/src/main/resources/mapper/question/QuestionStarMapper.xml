<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itellyou.dao.question.QuestionStarDao">
    <sql id="columns">
        ${alias}question_id ${prefix}question_id,
        ${alias}created_time ${prefix}created_time,
        ${alias}created_ip ${prefix}created_ip,
        ${alias}created_user_id ${prefix}created_user_id
    </sql>
    <insert id="insert">
        insert into question_star (question_id,created_time,created_ip,created_user_id) values (#{questionId},#{createdTime},#{createdIp},#{createdUserId})
    </insert>
    <delete id="delete">
        delete from question_star where question_id=#{questionId} and created_user_id=#{userId}
    </delete>
    <resultMap id="searchMap" type="QuestionStarDetailModel" autoMapping="true">
        <association property="question"  notNullColumn="id" javaType="QuestionDetailModel" columnPrefix="q_" autoMapping="true">
            <id property="id" column="id" />
        </association>
        <association property="user"  notNullColumn="id" javaType="UserInfoModel" columnPrefix="u_" autoMapping="true">
            <id property="id" column="id" />
        </association>
    </resultMap>
    <sql id="where">
        <where>
            <if test="beginTime != null">
                and ${alias}created_time <![CDATA[>=]]> #{beginTime}
            </if>
            <if test="endTime != null">
                and ${alias}created_time <![CDATA[<=]]> #{endTime}
            </if>
            <if test="ip != null">
                and ${alias}created_ip = #{ip}
            </if>
            <if test="questionId != null">
                and ${alias}question_id = #{questionId}
            </if>
            <if test="userId != null">
                and ${alias}created_user_id = #{userId}
            </if>
        </where>
    </sql>
    <select id="search" resultMap="searchMap">
        select
        <include refid="columns">
            <property name="alias" value="s."/>
            <property name="prefix" value=""/>
        </include>,
        <include refid="com.itellyou.dao.question.QuestionInfoDao.columns">
            <property name="alias" value="q."/>
            <property name="prefix" value="q_"/>
        </include>,1 q_use_star,
        v.title q_title,v.description q_description,v.reward_type q_reward_type,v.reward_value q_reward_value,v.reward_add q_reward_add,1 q_use_star,
        <include refid="com.itellyou.dao.user.UserInfoDao.columns">
            <property name="alias" value="u."/>
            <property name="prefix" value="u_"/>
        </include>
        from question_star s
        inner join question_info q on q.id = s.question_id
        inner join question_version v on v.question_id = q.id and v.version = q.version
        left join user_info u on u.id = s.created_user_id
        <include refid="where">
            <property name="alias" value="s."/>
        </include>
        <if test="order != null">
            <foreach collection="order" index="key" item="value" open="order by " separator=",">
                <choose>
                    <when test="key.indexOf('.') >= 0">${key} ${value}</when>
                    <otherwise>s.${key} ${value}</otherwise>
                </choose>
            </foreach>
        </if>
        <if test="limit != null and offset != null">
            limit #{offset},#{limit}
        </if>
    </select>
    <select id="count" resultType="Integer">
        select count(*)
        from question_star s
        inner join question_info q on q.id = s.question_id
        <include refid="where">
            <property name="alias" value="s."/>
        </include>
    </select>
</mapper>